# Keep the Docker build context lean — none of these are needed to build the
# panel binary or assemble the Pterodactyl image.
.git
.github
.ai
media
to-delete
windows_files
target
tmp
build
.vscode
*.log
